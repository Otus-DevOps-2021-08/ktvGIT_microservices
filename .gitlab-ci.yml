stages:
  - build
  - test
  - deploy

variables:
 DATABASE_URL: 'mongodb://mongo/user_posts'
before_script:
- cd reddit
- apt-get update && apt-get install -y mongodb-server ruby-full ruby-dev build-essential git && gem install bundler

build_job:
  stage: build
  script:
    - echo 'Building'


test_integration_job:
  stage: test
  script:
    - echo 'Testing 2'

deploy_job:
  stage: deploy
  script:
    - echo 'Deploy'
